% Path to the bibliography files
\addbibresource{bibliography/bibliography.bib}

% These commands change the chapter heading style a bit. 
\addtokomafont{chapter}{\raggedleft\Huge}
\renewcommand*{\chapterformat}{%
\vspace{1cm}\Large\chapappifchapterprefix{\nobreakspace}\thechapter
\autodot}

% Reformat the captions a littl bit, so that they are left aligned, and
% indented by 1em
\setkomafont{captionlabel}{\bfseries}
\renewcommand*{\captionformat}{~~}
\setcapindent{1em}
\setcapwidth[l]{\textwidth}
\setcapmargin[0em]{1em}

% adjust the line spread. This might change for different fonts
\linespread{1.05}

% small head
\addtokomafont{pagehead}{\small}
